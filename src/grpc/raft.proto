syntax = "proto3";
option go_package = "github.com/andrew-r-thomas/balsa";

service Raft {
  rpc Hello(HelloRequest) returns (HelloResponse) {}
}

message HelloRequest { string name = 1; }
message HelloResponse { string response = 1; }
